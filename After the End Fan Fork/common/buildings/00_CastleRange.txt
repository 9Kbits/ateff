 # Triggers are in Province scope. The From Scope contains the settlement holder (character)
 # start trigger is in title scope.
 
 #<tag> = {
 #	desc = description used in the tooltip of the building
 #	trigger = used for tech requirement of the building, set to 0 if no requirement so it can be build automatically on new games
 #	gold_cost = cost to build the building
 #	build_time = time to build, in days
 #	
 #	ai_creation_factor = weight that the AI uses to determine what building to build
 #	extra_tech_building_start = when starting a new game this building will be pre-built if the province has higher tech than the requirement + this + random 0-1
 #}
 
 # Castle
castle = {
	# All cultural buildings follow the same pattern:
	# tech required is 0, 1, 2, 4 for the levels of the buildings
	# cost is 200 + 100 per level
	# 60 maintenance of troops + 20 per level
	# + 0.15 bonus in either offensive, defensive or morale sometimes split over different units or stats same on all levels
	
	# Culture building names MUST start with ca_culture

	ca_orf_range_marshal_1 = {
		desc = orf_range_marshal_1_desc
		potential = {
			FROM = {
				government = open_range_feudalism_government
			}
			FROMFROM = { is_capital = yes }
		}
		is_active_trigger = {
			FROM = {
				government = open_range_feudalism_government
			}
			FROMFROM = { is_capital = yes }
		} 
		trigger = { 
			FROM = {
				government = open_range_feudalism_government
			}
			FROMFROM = { is_capital = yes }
		}
		gold_cost = 2
		build_time = 2
		cowboys = 100
		light_cavalry_offensive = 0.15
		ai_creation_factor = 101
		
		extra_tech_building_start = 0.8
	}
	ca_orf_range_marshal_2 = {
		desc = ca_orf_range_marshal_2_desc
		potential = {
			FROM = {
				government = open_range_feudalism_government
			}
			FROMFROM = { is_capital = yes }
		}
		is_active_trigger = {
			FROM = {
				top_liege = { has_law = range_senate_size_0 }
			}
			ROOT = { num_of_empty_holdings = 0 }
			FROMFROM = { is_capital = yes }	
		} 
		trigger = { 
			FROM = {
				top_liege = { has_law = range_senate_size_0 }
			}
			ROOT = { num_of_empty_holdings = 0 }
			FROMFROM = { is_capital = yes }
		}
		upgrades_from = ca_orf_range_marshal_1
		gold_cost = 2
		build_time = 2
		cowboys = 50
		light_cavalry = 40
		light_cavalry_offensive = 0.15
		ai_creation_factor = 100
		
		extra_tech_building_start = 0.8
	}
}


