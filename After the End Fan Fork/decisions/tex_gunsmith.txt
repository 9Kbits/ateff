decisions = {
	hunt_gunsmith = {
		is_high_prio = yes
		potential = {
			is_alternate_start = no
			is_playable = yes
			independent = yes
			culture_group = west_anglo
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { trait = incapable }
			prestige = 500
			NOT = { 
				OR = {
					has_character_flag = searching_for_smith
					has_character_flag = searching_for_smith_cooldown
				}
			}
		}
		effect = {
		set_character_flag = searching_for_gunsmith
		hidden_tooltip = { narrative_event = { id = TX.20001 } }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 1
		}
	}
}